# Кладётся в /etc/nginx/conf.d/shtil-limits.conf (контекст http). Ограничение частоты запросов к Штиль.
limit_req_zone $binary_remote_addr zone=shtil_api:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=shtil_web:10m rate=30r/s;
